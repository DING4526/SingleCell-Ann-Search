# 软件工程大作业：单细胞 ANN 检索系统

> 根据《团队作业.pptx》整理。本文档将 PPT 内容按项目背景、数据说明、系统功能、技术方案与提交要求重新组织，便于后续开发、汇报和撰写项目文档。

---

## 目录

1. [作业目标](#1-作业目标)
2. [项目背景](#2-项目背景)
3. [数据说明](#3-数据说明)
4. [数据处理与降维](#4-数据处理与降维)
5. [系统功能模块](#5-系统功能模块)
6. [ANN 算法与检索库](#6-ann-算法与检索库)
7. [开发要求](#7-开发要求)
8. [中期提交要求](#8-中期提交要求)
9. [结项提交要求](#9-结项提交要求)
10. [软件开发文档要求](#10-软件开发文档要求)
11. [最终提交材料](#11-最终提交材料)
12. [项目开发建议清单](#12-项目开发建议清单)

---

## 1. 作业目标

本次软件工程大作业要求开发一个**面向单细胞高维向量数据的近似最近邻（Approximate Nearest Neighbor, ANN）检索系统**，为大规模单细胞数据提供高效的相似样本检索能力。

系统需要支持以下核心能力：

- 单细胞数据读取与预处理；
- 精确检索与近似检索；
- 多种索引结构管理；
- 查询结果展示；
- 性能评测；
- 通过 Web 页面进行交互式查询。

用户可以在 Web 页面中输入查询细胞或查询向量，设置检索参数，获取 Top-K 相似结果，并查看查询耗时、索引状态和评测指标等信息。

---

## 2. 项目背景

### 2.1 单细胞测序技术

单细胞测序技术是指将组织中的单个细胞分离出来，分别测量每个细胞内部的分子信息，并通过测序读取这些信息。

测序完成后，可以统计每个细胞中各个基因的表达量，从而得到一个：

> **细胞 × 基因** 的表达矩阵

在该矩阵中：

- 每一行表示一个细胞；
- 每一列表示一个基因；
- 矩阵中的数值表示对应基因在对应细胞中的表达量。

### 2.2 为什么需要 ANN 检索

随着单细胞测序技术的发展，单细胞数据规模不断扩大，一个实验往往可以产生数十万个细胞样本。经过数值化表示后，每个细胞都可以看作一个高维向量。

传统的精确最近邻搜索虽然能够返回准确结果，但在数据规模较大、维度较高时，通常存在以下问题：

- 查询效率低；
- 响应时间长；
- 计算资源消耗大。

近似最近邻检索（ANN）可以在保证较高检索质量的同时显著降低查询开销，因此已经广泛应用于图像搜索、推荐系统、语义检索等场景，也适合用于单细胞数据相似样本检索。

---

## 3. 数据说明

### 3.1 数据格式：AnnData（`.h5ad`）

在 Python 单细胞分析生态中，最常用的数据结构为 **AnnData**，其文件后缀为 `.h5ad`。

AnnData 的主要字段如下：

| 字段 | 含义 |
|---|---|
| `X` | 基因表达矩阵 |
| `obs` | 细胞元信息 |
| `var` | 基因元信息 |
| `obsm` | 降维后的特征，例如 PCA、UMAP |
| `layers` | 不同处理阶段的数据 |

其中，`X` 是 AnnData 最核心的数据部分：

- 行：细胞（cell）；
- 列：基因（gene）；
- 值：基因表达量。

### 3.2 数据集来源

本次作业使用的数据集来源于 CZI（Chan Zuckerberg Initiative）细胞科学项目，研究主题为：

> **Single-Cell Atlas Of Human Pediatric Liver Reveals Age-Related Hepatic Gene Signatures**

数据集内容为健康儿童和成人肝脏组织的单细胞转录组数据。

### 3.3 可关注的数据分类字段

PPT 中提示可关注以下分类方式或元数据字段，用于后续条件检索、可视化或结果过滤：

- `assay`
- `cell_type`
- `development_stage`
- `donor_id`
- `AgeGroup`
- `Dissociation`
- `donor_age`

---

## 4. 数据处理与降维

常见的单细胞数据处理流程如下：

```text
原始表达矩阵
    ↓
质量控制（QC）
    ↓
数据标准化
    ↓
对数变换
    ↓
高变基因筛选
    ↓
数据缩放
    ↓
PCA 降维
    ↓
生成低维细胞向量
```

本次作业提供的数据已经经过处理。如果需要进行其他处理，可以在原始数据上进一步处理。

PPT 中使用的降维方法是 **PCA**。也可以尝试其他方式生成 embedding，例如：

- 自编码器；
- scVI；
- 其他深度学习或表示学习方法。

但需要注意：盲目使用其他方法生成 embedding 未必会带来更好的检索效果，应结合实验结果进行取舍。

---

## 5. 系统功能模块

系统可以按以下模块进行设计与实现。

### 5.1 用户信息模块

功能要求：

- 用户可以通过账号和密码注册、登录系统；
- 用户登录后可以查看可视化系统；
- 管理员可以进行用户管理等操作。

### 5.2 数据管理模块

该模块负责单细胞数据的导入、读取和组织管理。

主要功能包括：

- 导入单细胞数据；
- 读取 `.h5ad` 数据文件；
- 将单细胞样本表示为高维向量；
- 对输入数据进行格式校验；
- 对数据进行基础预处理；
- 管理数据集的增加、删除和切换。

### 5.3 索引构建模块

该模块负责根据输入数据建立检索索引，为高效查询提供支持。

主要功能包括：

- 构建 ANN 检索索引；
- 保存索引；
- 加载索引；
- 动态管理索引；
- 根据数据集变化更新索引。

### 5.4 查询检索模块

该模块是系统的核心模块之一。

主要功能包括：

- 接收用户输入的查询细胞编号；
- 接收用户输入的查询向量；
- 按照设定参数执行相似性检索；
- 返回 Top-K 个最相近的结果；
- 支持条件检索，例如限定具体细胞类型；
- 返回对应细胞信息。

需要支持的基本检索参数包括：

- 索引类型；
- 距离度量方式；
- 返回结果数量 Top-K。

### 5.5 可视化展示模块

该模块负责将检索结果以直观形式展示给用户。

建议展示内容包括：

- Top-K 相似细胞列表；
- 相似度或距离分数；
- 查询耗时；
- 细胞元信息；
- 索引状态；
- 性能评测指标；
- UMAP / PCA 等二维可视化结果。

---

## 6. ANN 算法与检索库

### 6.1 常见 ANN 算法

| 类型 | 代表算法 |
|---|---|
| 哈希方法 | LSH |
| 聚类索引 | IVF |
| 图结构 | HNSW、NSG |
| 向量压缩 | PQ、OPQ |
| 混合结构 | IVF+PQ、IVF+HNSW |

### 6.2 典型算法说明

#### IVF：Inverted File Index

IVF 的基本思想是：

> 先聚类，再在局部区域搜索。

适合用于降低搜索范围，提高查询效率。

#### HNSW：Hierarchical Navigable Small World

HNSW 基于图结构构建近邻网络。

特点：

- 召回率高；
- 查询效率高；
- 是当前主流 ANN 方法之一。

#### PQ：Product Quantization

PQ 通过向量压缩编码降低存储成本。

特点：

- 降低内存占用；
- 适合超大规模数据；
- 常与 IVF 等方法组合使用。

### 6.3 常见 ANN 检索库

| 检索库 | 主流算法 |
|---|---|
| FAISS | IVF / PQ / HNSW |
| HNSWLIB | HNSW |
| Qdrant（向量数据库） | HNSW |

#### FAISS

FAISS 是 Facebook AI Research 提供的高性能向量检索库。

特点：

- 支持 GPU 加速；
- 支持 IVF / PQ / HNSW；
- 适合大规模向量搜索。

#### HNSWLIB

HNSWLIB 是轻量级 ANN 检索库。

特点：

- 查询速度快；
- 内存效率高；
- 部署简单。

#### 向量数据库

向量数据库可以理解为：

> ANN 检索 + 数据库系统

除了向量检索外，向量数据库通常还支持元数据管理等数据库功能。

常见向量数据库包括：

- Milvus；
- Qdrant；
- Weaviate；
- ChromaDB。

---

## 7. 开发要求

本次作业要求实现为 **Web 应用**。

技术栈可以自行选择。如果没有 Web 开发经验，建议使用：

- Flask；
- Python 后端；
- 简单前端页面；
- FAISS 或 HNSWLIB 作为 ANN 检索库。

系统部署要求：

- 使用 Git 进行代码版本管理；
- 项目代码提交到 GitHub；
- GitHub 提交记录应能体现项目完成过程。

---

## 8. 中期提交要求

### 8.1 提交与展示要求

中期提交要求：

- 项目代码提交到 GitHub；
- GitHub 需要有提交记录，体现项目完成过程；
- 每个小组进行作业展示；
- 展示时间为 6 月 1 日和 6 月 8 日两次课；
- 展示按组号排序。

每组展示时长：

> 5–8 分钟

展示内容包括：

- 已完成功能讲解；
- 系统界面演示；
- 演示可以现场进行，也可以使用录制视频。

### 8.2 中期功能要求

中期至少需要实现以下功能：

- 单细胞数据读取；
- 数据向量化表示；
- ANN 索引构建；
- 相似细胞检索；
- 至少实现一种 ANN 算法或检索库；
- 支持 Top-K 相似细胞搜索，并返回对应细胞信息。

### 8.3 中期评分方式

中期提交根据完成度打分。

分数分配如下：

| 项目 | 分值 |
|---|---:|
| 项目完成度 | 15 分 |
| 展示 | 5 分 |
| 合计 | 20 分 |

### 8.4 贡献度说明

小组得分计算方式：

```text
小组得分 = 给分 × 小组人数
```

例如：

```text
17 × 5
18 × 4
```

个人得分计算方式：

```text
个人得分 = 贡献度 × 小组得分
```

例如：

```text
0.21 × 17 × 5
0.23 × 18 × 4
```

注意事项：

- 作业得分上限为 20 分；
- 个人得分即使超出上限，也只取上限；
- 贡献度分配应合理，不要过于极端；
- 组长分配贡献度时，需要让组员知情并同意。

---

## 9. 结项提交要求

### 9.1 基础要求

结项时需要完成一个可运行的单细胞 ANN 检索系统。

具体要求如下：

#### 1. 系统功能完整

系统应完成一个可运行的单细胞 ANN 检索系统。

#### 2. 条件检索

系统应能根据输入细胞和条件要求返回 Top-K 相似细胞。

条件示例：

- 限定某具体细胞类型；
- 限定某类元数据字段；
- 在指定数据子集内检索。

#### 3. 实验评估

需要对系统性能进行分析，包括但不限于：

- 查询响应时间；
- 检索召回率；
- 不同索引参数下的性能变化；
- 内存占用；
- 构建索引耗时。

#### 4. 可视化展示

系统需提供至少一种可视化结果。

鼓励提供简单交互方式，例如：

- 在可视化结果上直接选择细胞进行查询；
- 在 UMAP 图中展示查询细胞和相似细胞；
- 使用颜色区分细胞类型、年龄组或其他元数据。

#### 5. 数据集管理

系统要求可以进行数据集管理，包括：

- 增加数据集；
- 删除数据集；
- 查看数据集状态；
- 动态进行索引管理。

### 9.2 加分项

加分项视完成情况与实现难易程度加分，不完全按照结果给分，鼓励进行尝试。

加分规则：

- 基础要求以外的其他功能可视情况加分；
- 加分是在总得分基础上加分；
- 加分上限为每人 2 分。

可选加分方向如下：

#### 多数据集联合检索

支持多个单细胞数据集联合建立索引，实现跨数据集细胞搜索。

#### ANN 算法改进

对现有 ANN 算法进行改进，在以下方面获得改进效果：

- 检索精度；
- 查询时间；
- 内存占用。

#### RAG + 单细胞数据库

实现自然语言查询细胞，使用 AI 辅助细胞分析。

---

## 10. 软件开发文档要求

结项提交时需要完成软件开发文档，建议包含以下内容。

### 一、项目概述

- 开发背景；
- 项目目标；
- 开发环境；
- 可行性分析；
- 项目计划。

### 二、需求分析与系统设计

- 需求分析；
- 系统设计；
- 详细设计；
- 数据库设计；
- UI 设计。

### 三、系统测试

- 测试环境；
- 功能测试；
- 性能测试。

### 四、项目管理

- 参与人员及分工；
- 项目进展记录；
- 项目管理工具。

### 五、用户手册

- 系统安装说明；
- 系统启动方式；
- 数据导入说明；
- 检索功能使用说明；
- 可视化功能使用说明；
- 常见问题说明。

---

## 11. 最终提交材料

组长需要提交以下材料到指定链接：

- 源代码；
- 开发文档；
- 演示视频。

演示视频要求：

- 不需要讲解代码；
- 需要完整演示系统功能。

项目代码要求：

- 提交到 GitHub；
- 包括完整前后端代码；
- 包括安装运行说明；
- 建议有足够的代码注释；
- 项目链接需要在项目报告中给出。

提交截止时间：

> 2024 年 7 月 12 日 24 点

如需要提前打分，可联系助教。

---

## 12. 项目开发建议清单

为了便于小组推进，可以将任务拆分为以下开发清单。

### 12.1 数据处理

- [ ] 读取 `.h5ad` 文件；
- [ ] 查看细胞数量和基因数量；
- [ ] 提取 PCA 向量或其他 embedding；
- [ ] 读取细胞元信息；
- [ ] 支持按细胞类型、年龄组等字段过滤。

### 12.2 检索功能

- [ ] 实现精确最近邻检索作为 baseline；
- [ ] 实现至少一种 ANN 检索方法；
- [ ] 支持 Top-K 查询；
- [ ] 支持查询细胞编号；
- [ ] 支持查询向量；
- [ ] 返回相似细胞及其元信息。

### 12.3 索引管理

- [ ] 构建索引；
- [ ] 保存索引；
- [ ] 加载索引；
- [ ] 查看索引状态；
- [ ] 数据集变化后更新索引。

### 12.4 Web 系统

- [ ] 实现用户登录注册；
- [ ] 实现数据集管理页面；
- [ ] 实现检索参数设置页面；
- [ ] 实现结果展示页面；
- [ ] 实现性能指标展示页面；
- [ ] 实现至少一种可视化图。

### 12.5 实验评估

- [ ] 统计查询时间；
- [ ] 统计索引构建时间；
- [ ] 对比精确检索与 ANN 检索结果；
- [ ] 计算召回率；
- [ ] 分析不同参数对检索性能的影响。

### 12.6 项目管理与提交

- [ ] 使用 Git 管理代码；
- [ ] 保持 GitHub 提交记录；
- [ ] 编写 README；
- [ ] 编写开发文档；
- [ ] 录制演示视频；
- [ ] 在报告中附上 GitHub 链接。
